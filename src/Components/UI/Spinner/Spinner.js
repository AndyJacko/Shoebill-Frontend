import React from "react";

import "./Spinner.css";

const Spinner = () => {
  return (
    <div className="padding-20">
      <span className="loader"></span>
    </div>
  );
};

export default Spinner;
